# DataPilotAgent 待实现功能清单

**版本**：v1.1
**日期**：2026-08-30
**依据**：`docs/需求PRD.md v1.2`、`docs/Block与协议规范.md`、`docs/技术方案设计.md`、`docs/定时任务与第三方通知方案.md`，对照前后端实际实现盘点。

> 状态图例：🔴 未实现 ｜ 🟡 部分实现 ｜ 🟢 已实现（列入基线，不再安排）

---

## P0 — 对话核心闭环（用户直接感知，先做）

| # | 功能 | 契约出处 | 缺口说明 | 复杂度 | 状态 |
|---|------|---------|---------|--------|------|
| 1 | 追问建议可点击 | PRD 3.1.6 / 验收「追问建议」 | 🔴 suggestions 按钮无 onClick，点击无反应；点击应带问句发起新对话 | S | ✅ 已实现 |
| 2 | SQL 编辑 + 执行闭环 | PRD 3.1.3 / 验收「SQL 编辑」 | 🔴 code block 只读；缺 [编辑][执行]、执行状态与结果回填 | M | ✅ 已实现 |
| 3 | 重新生成 | PRD 5.1 | 🔴 assistant 消息无「重新生成」入口（保留原回复） | S | ✅ 已实现 |
| 4 | 表格增强 | PRD 3.1.2 / 11.1（10 万行） | 🟡 仅前 50 行 + CSV 导出；缺排序/筛选/分页/Excel-JSON 导出 | M | ✅ 排序/筛选/虚拟滚动/三格式导出 |

> 2026-08-26：P0 四项已完成（#1 suggestions 点击发起追问；#2 前后端 code block 编辑执行闭环——Monaco 只读高亮回显 + 可编辑，SELECT 回填 execution + 追加结果表，写操作安全拦截；#3 assistant 重新生成；#4 表格排序/筛选/虚拟滚动 + CSV/Excel/JSON 导出）。均通过前端 tsc/oxlint/build、后端 execute 冒烟与浏览器功能测试。其中 #4 虚拟滚动（PRD 11.1）已实测：10 万行表格仅渲染 26 行 DOM（视口 440px，滚动到中部正确渲染 ~50000 行区间），滚动/筛选/排序正常，测试数据已清理。

> 2026-08-26：P1 首批三项已完成（#5 Composer「使用模板」入口——弹窗选择模板后 SQL 回填输入框、联动会话数据源；#7 Sidebar 会话重命名——内联输入 + Enter/Esc/blur 提交、`POST /sessions/update`；#8 全 Block 一键复制——hover 悬浮复制按钮，text/table(TSV)/chart/insights/confirmation/error 覆盖，code 沿用自带复制避免重复）。均通过 tsc/oxlint/build 三关与浏览器功能测试。

> 2026-08-27：#6 附件预览/替换/移除已完成——后端新增 `GET /api/upload/{id}/preview`（会话级 SQLite 只读前 N 行，默认 50）、`POST /api/upload/{id}/replace`（新文件建新附件+任务，旧记录保留供溯源，`replaced_from` 回填）、`POST /api/upload/delete`（删记录+MinIO 对象+临时表）；前端 AttachmentBlock 就绪后提供预览（内嵌 50 行表格）/替换（引用切新附件+轮询解析）/移除（confirm→已移除标记）三动作，草稿区移除接入后端删除，轮询逻辑抽取为 `useAttachmentPolling` 复用。后端 httpx 冒烟 19 项全 PASS，浏览器实测预览展开 8 行表格、移除后 block 标记「已移除」且后端 404（替换按钮存在，UI 文件选择受自动化环境限制，其管道与冒烟覆盖一致）。

> 2026-08-27：#9 图表补齐已完成——`ChartBlock.tsx` 重写：① 热力图用自定义 SVG 网格渲染（`matrix.values` 归一化映射 `--chart-1..5` 五档色阶，Y/X 轴类别标签 + hover `<title>` + 低→高图例，recharts 无原生支持，保留 SVG 导出管道同源）；② 图表配置修改弹窗（标题/X/Y 轴标签输入 + 每系列 chart-1..5 色板单选，本地展示层生效——契约 6.2 chart 为终态无更新字段；卡片标题渲染移入 ChartBlock 跟随配置）；③ PDF 导出（新增 `jspdf` 依赖，SVG→canvas→PNG→jsPDF 管道，`await import('jspdf')` 按需加载独立 chunk 约 130KB gzip）；另补齐 Bar/Line/Scatter 的 X 轴标签渲染（原仅 Y 轴），并对缺失 `series` 的 heatmap 内容容错。tsc/oxlint/build 三关通过，浏览器实测：热力图 20 格渲染 + 图例、配置修改（销售额 chart-1→chart-3、X 轴「时间」、Y 轴保留）即时生效、折线图与热力图 PDF 均导出有效 PDF（1 页）。

> 2026-08-27：#11 澄清机制 UI 已完成——后端 SYSTEM_PROMPT 明确"问题缺少必要参数（时间范围、指标口径等）时先澄清再行动"，澄清时输出文本 + 「可点击选项：」编号选项区，`chat_service` 新增 `_extract_suggestions` 解析（正则提取编号+文案）与 `_suggestions_block` 生成器（契约 2.7 items[{text,message}]），成功路径将选项块落库并随 SSE 下发；前端沿用现有 suggestions 渲染（rounded-full 可点击按钮）。实测发「按时间趋势分析」→ 落库 blocks 含 suggestions 类型、4 个可点击选项，浏览器点击可用。

> 2026-08-27：#12 SQL 历史 + 结果溯源已完成——新增 `frontend/src/lib/sqlRecords.ts`（`collectSql` 遍历消息 blocks 收集 code/table/chart 三类 SQL，因 oxlint only-export-components 独立成文件）；`SqlHistoryDialog`（Header 工具栏 History 按钮，计数角标，按类别徽标展开/折叠 + 复制）；`SqlQueryDialog`（单条 SQL 查看弹窗）；TableBlock/ChartBlock 工具条各加「查看查询 SQL」按钮（仅当 content.query 存在）。实测会话内 3 条记录（SQL 执行/表格/图表）齐全，表格/图表溯源弹窗均正常。

> 2026-08-27：对话流体验三项已完成——① 引用来源/证据链（新 block 类型 `sources`，契约 2.11）：服务端从工具执行轨迹确定性推导（数据源名 + SQL FROM/JOIN 表名 + 去重查询），前端 SourcesBlock 渲染 chips，sql 项点击复用 SqlQueryDialog；② 追问建议动态化：提示词要求每次分析后输出「可点击选项：」2-3 个基于当前结果的下一步选项，实测建议随上下文自适应（按周后自动给「生成折线图/改按月」）；③ 会话内指代修正：提示词声明解析『改成/不是/用上次』指代上一轮，Composer 命中关键词提示「将基于上一轮分析结果执行」，实测「改成按周统计」→ SQL 变 GROUP BY week。三者均通过 SSE/落库验证，前端三关 0 errors。

## P1 — 产品差异化（契约明确列出）

| # | 功能 | 契约出处 | 缺口说明 | 复杂度 | 状态 |
|---|------|---------|---------|--------|------|
| 5 | 模板「使用模板」入口 | PRD 补充 / 技术方案 2.3 | 🟡 模板 CRUD 已闭环，但聊天输入框无入口，模板是孤岛、未被真正使用 | S | ✅ 已实现 |
| 6 | 附件预览 / 替换 / 移除 | PRD 3.1.5 | 🟡 `frontend/src/pages/chat/AttachmentBlock.tsx` 仅状态展示；缺前 N 行预览、替换（关联分析更新）、移除 | M | ✅ 已实现 |
| 7 | 会话重命名 | PRD 5.5 | 🟡 后端 `POST /sessions/update` 已存在，前端 Sidebar 无重命名入口（现仅删除） | S | ✅ 已实现 |
| 8 | Block 复制 | PRD 5.1 | 🔴 所有 Block「一键复制」未实现 | S | ✅ 已实现 |
| 9 | 图表补齐 | PRD 3.3.1-3.3.3 | 🟡 热力图是占位文案；缺图表配置修改（颜色/标签/轴/标题）、PDF 导出（现仅 PNG/SVG） | M | ✅ 已实现 |
| 10 | 数据源连接状态 + 数据新鲜度 | PRD 3.2.3-3.2.4 | 🟡 持续状态标记 + 最后连接时间展示已实现（调度器心跳写 status/last_checked_at，列表状态徽标 + 检测时间）；仍缺 Schema 变更检测、数据上传时间专门展示 | M | 🟡 部分实现 |
| 11 | 澄清机制 UI | PRD 3.1.7 | 🔴 模糊问题 AI 主动给可点击选项（不猜测执行）未落地 | M | ✅ 已实现 |
| 12 | SQL 历史 + 结果溯源 | PRD 5.2 | 🔴 会话内 SQL 统一查看、点击表格/图表看对应查询均未实现 | M | ✅ 已实现 |

> 2026-08-27：#18 任务取消/重试已完成——`useChat` 的 task_status 事件将终态写入 progress content（`cancelled/failed` 标记，因 block_end 会覆写 status）；BlockViewer progress 渲染：running 且 cancellable 时显示「取消」（POST `/api/tasks/{id}/cancel` + 乐观标记），终态显示「已取消/任务失败」+「重试」按钮。实测：probe 任务 10s running → cancel → cancelled（进度冻结）；UI 种子 progress 块点「取消」→ 显示已取消、百分比隐藏、重试按钮出现。

> 2026-08-27：#13/#14/#15/#16 全局体验小件已完成——#13 主题切换（`lib/theme.ts`，localStorage 持久化 + 系统偏好兜底，Header Sun/Moon 按钮，实测切暗色 body 背景 oklch(0.145) 且刷新保持）；#14 浏览器通知（`lib/notify.ts`，Header 铃铛请求权限，`useChat` 对耗时 ≥10s 的任务在 success/failed/cancelled 终态发通知，需用户手动授权一次）；#15 上下文接近上限提示（`lib/tokenEstimate.ts` 估算，≥80%×32k 时 Composer 上方警告横幅，实测 M2 联调3 显示「约 463,860 tokens」）；#16 对话导出 PDF（导出菜单新增「导出对话（PDF）」，按 PRD 走浏览器打印，print: 变体隐藏 Sidebar/Header/Composer/草稿区、消息流溢出可见）。

> 2026-08-30：盘点更新——① #10 数据源连接状态已部分实现：调度器 `_check_datasources` 心跳持续写回 status/last_checked_at/last_error/server_version，数据源列表渲染状态徽标（正常/异常/未检测 + 色点 + 失败原因/版本 tooltip）与「检测于 xx/从未检测」；仅余 Schema 变更检测与数据上传时间专门展示未做。② 定时任务泛化 + 第三方通知整块已实现（详见「建议执行顺序」第 5 条）：Automation 全链路（croniter 调度/`sql_report` 动作/运行历史/`/api/automations/parse` 自然语言创建）+ 通知渠道层（飞书/企微/钉钉 webhook + SMTP + 测试 + 发送记录）+ on_success/on_failure 自动通知 + `POST /api/notifications/send` 对话结果推送（前端 PushToChannelDialog）+ Automation/渠道管理前端页面，逐项列入基线。

## P2 — 全局体验打磨

| # | 功能 | 契约出处 | 缺口说明 | 复杂度 |
|---|------|---------|---------|--------|
| 13 | 明暗主题切换 | PRD 5.6 | 🔴 无切换入口 | S | ✅ 已实现 |
| 14 | 浏览器通知 | PRD 5.6 | 🔴 长任务完成/失败通知未实现 | S | ✅ 已实现 |
| 15 | 上下文接近上限提示 | PRD 3.1.1 | 🔴 未实现 | S | ✅ 已实现 |
| 16 | 对话导出 PDF | PRD 3.3.3 | 🟡 仅 Markdown；PDF 走浏览器打印另存 | S | ✅ 已实现 |
| 17 | 修改密码入口 | PRD 补充（首次登录改密） | 🔴 前端无入口 | S |
| 18 | progress 取消按钮 | PRD 3.1.2 / 补充「任务取消」 | 🟡 后端取消 API 存在，前端 progress block 未接取消/重试 | S | ✅ 已实现 |
| 19 | @ 提及数据集 | 输入框引导 | ❌ 已移除（2026-08-27 决定不做，前端按钮已删除） | — |
| 20 | 「始终允许此类操作」偏好 | PRD 3.1.4 | 🔴 确认卡片无该选项 | M |

---

## 已实现基线（不再安排，仅作记录）

登录/JWT、会话列表（搜索/删除/新建）、SSE 流式对话（token 缓冲、会话切换不中断）、全类型 Block 渲染、确认卡片（可编辑 SQL + 确认/拒绝闭环）、点赞/点踩反馈、Token 消耗展示、附件上传与状态流转、数据源选择器、数据源 CRUD/测试/预览、LLM 供应商/系统配置/审计日志、日志查询导出、模板 CRUD、对话 Markdown 导出、图表渲染（折线/柱状/饼/散点/热力图 + 本地配置修改）与 PNG/SVG/PDF 导出、表格 CSV 导出、**数据源连接状态徽标与最后连接时间展示（#10 部分）**、**定时任务（Automation：croniter 调度 / sql_report / 运行历史 / 自然语言创建）与第三方通知（飞书/企微/钉钉 webhook + SMTP / on_success+on_failure 告警 / 对话结果推送）全链路**。

---

## 建议执行顺序

1. ~~#1 → #2 → #3 → #5~~ ✅（小步闭环对话主线）
2. ~~随后 #7 / #8~~ ✅（顺手补齐）
3. 剩余 P1：#10 数据源连接状态已部分实现（状态徽标 + 最后连接时间）；仍缺口 Schema 变更检测、数据上传时间专门展示（M）
4. P2 剩余：#17 修改密码入口（S）、#20 「始终允许」偏好（M），按里程碑节奏排入 M5 收尾；#19 @ 提及数据集已移除不做
5. **定时任务泛化 + 第三方通知（方案见 `docs/定时任务与第三方通知方案.md`，契约已回写至《技术方案设计.md》 2.2/2.3/2.4）——✅ 已实现（2026-08-30 盘点确认）**：
   - Automation 骨架 ✅：表 + service + croniter + 调度器扩展 + `sql_report` 动作 + 运行历史 + API；
   - 通知渠道层 ✅：Channel + Notifier 抽象 + 飞书/企微/钉钉 webhook + SMTP + 测试 + 发送记录；
   - 通知接入 ✅：Automation 成功/失败自动通知（on_success/on_failure）；
   - 对话结果主动推送 ✅：`POST /api/notifications/send` + 前端 PushToChannelDialog；
   - 自然语言创建 ✅：`/api/automations/parse` + LLM 严格 JSON + 二次确认 UI（AutomationParseDialog，数据源由用户确认选择）；
   - 前端页面 ✅：Automation 列表/表单/运行历史、渠道管理、parse 确认弹窗、发送记录。
